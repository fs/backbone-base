#!/bin/bash

# Make sure we have gulp installed and have the latest version
if ! which gulp; then
  npm install -g gulp
else
  npm update -g gulp
fi

# Install all dependencies
npm install --ignore-scripts

# Setup configurable environment variables
if [ ! -f .env ]; then
  cp .env.example .env
fi
